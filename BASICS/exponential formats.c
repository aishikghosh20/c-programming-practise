#include <stdio.h>
int main()
{double large = 1223344555.34565444;
double small= 0.00000000000002;

printf("\n The large number in exponential format is: %e", large);
printf("\n The small number in exponential format is: %e", small);
return 0;
}

// exercises 11 to 15 & exercises 17 and 18 - are left
