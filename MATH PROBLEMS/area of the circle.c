#include <stdio.h>
int main()
{float radius;
printf("enter the radius: ");
scanf("%f", &radius);
float area = 3.1415 * radius * radius;
printf("\n The Area of the circle is : %f", area);
return 0;
}
