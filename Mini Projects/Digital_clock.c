#include<stdio.h>
#include <time.h>

int main ()
{
    // Creates a variable to store the time from the system
    time_t rawtime; 

    // using the function to get the system time 
    time(&rawtime); //stores the time in the address of the variable

}